## What changed

<!-- The change itself, in a few sentences. -->

## Why

<!-- The reason the change is worth making. Link the architecture decision
     record if the change introduces one. -->

## How it was verified

<!-- Commands run and tests added, naming the category: unit, property,
     validation or regression. A change that affects performance reports before
     and after figures with the machine state they were taken on.

     The definition of done is in CONTRIBUTING.md. It is not repeated here,
     because a checklist that is ticked without being read is worse than the
     document it was copied from. -->
